# Shared

Canonical source for reusable components and styles across sakhalteam projects.

## Usage

When starting a new project repo, copy what you need from here into that project's `src/`.
If you improve something in a project, bring the updated version back here.

## Structure

- `components/` — reusable React components
- `styles/` — shared CSS (variables, resets, utility classes)
